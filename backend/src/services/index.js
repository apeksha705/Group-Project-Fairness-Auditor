/**
 * Services index.
 * Export all service modules from here as they are created.
 *
 * Example:
 *   module.exports = {
 *     projectService: require('./project.service'),
 *     contributionService: require('./contribution.service'),
 *   };
 */

module.exports = {};
